{% import "header.jinja" as header -%}

<!DOCTYPE html>
<head>
    <link href="{{ url_for('static', filename='style.css') }}" rel="stylesheet">
    {% block header %}{% endblock header %}
</head>
<body>
    {{ header.navbar() }}
    <div class="body">
        {{ header.title_bar('index') }}
        <main>
            {% block body %}{% endblock body %}
        </main>
    </div>
</body>
